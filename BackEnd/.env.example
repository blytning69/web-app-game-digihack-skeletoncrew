# Supabase Postgres connection string
DATABASE_URL=postgresql://postgres:admin@db.swcljhhuvctrxdsujofz.supabase.co:5432/postgres
?sslmode=require

# Backend server port (Railway will set this automatically, but keep fallback for local dev)
PORT=4000

# JWT secret (if you later add auth logic)
JWT_SECRET=supersecret123
